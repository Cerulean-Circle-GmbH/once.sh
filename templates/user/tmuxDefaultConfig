# ============================================================================
# tmux.conf - tmux configuration for oosh/otmux
# Generated by: otmux config.init
# Features: mouse, OS-independent clipboard, pane headers, color support
# ============================================================================

# ─────────────────────────────────────────────────────────────────────────────
# MOUSE SUPPORT
# ─────────────────────────────────────────────────────────────────────────────

# Enable full mouse support
set -g mouse on

# ─────────────────────────────────────────────────────────────────────────────
# PANE TITLE HEADERS
# ─────────────────────────────────────────────────────────────────────────────

# Show pane title at top of each pane border, bold if active
set -g pane-border-status top
set -g pane-border-format " #{?pane_active,#[bold],}#T#[default] "

# ─────────────────────────────────────────────────────────────────────────────
# CLIPBOARD — OS-independent
# ─────────────────────────────────────────────────────────────────────────────

# Enable clipboard passthrough
set -g set-clipboard on

# Copy mode key bindings (vi style)
setw -g mode-keys vi

# OS-specific clipboard bindings (copy/paste) are applied at runtime by:
#   otmux setup.default
# This detects the OS and configures the correct clipboard tool:
#   macOS: pbcopy/pbpaste | Linux: xclip/xsel | Termux: termux-clipboard

# ─────────────────────────────────────────────────────────────────────────────
# TERMINAL SETTINGS
# ─────────────────────────────────────────────────────────────────────────────

# Use xterm-256color for broad compatibility
set -g default-terminal "xterm-256color"

# Enable true color support
set -ga terminal-overrides ",xterm-256color:Tc"

# Set color environment variables for Claude Code and other apps
# FORCE_COLOR=1 ensures colors even if NO_COLOR is set elsewhere
set-environment -g COLORTERM truecolor
set-environment -g FORCE_COLOR 1

# Faster escape time (better for vim/editors)
set -s escape-time 10

# Large scrollback
set -g history-limit 50000

# ─────────────────────────────────────────────────────────────────────────────
# WINDOW & PANE SETTINGS
# ─────────────────────────────────────────────────────────────────────────────

# Renumber windows when one is closed
set -g renumber-windows on

# ─────────────────────────────────────────────────────────────────────────────
# KEY BINDINGS
# ─────────────────────────────────────────────────────────────────────────────

# Reload config with prefix + r
bind r source-file ~/.tmux.conf \; display-message "Config reloaded!"

# Split panes with | and - (more intuitive)
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"

# New windows/panes inherit current path
bind c new-window -c "#{pane_current_path}"

# Navigate panes with vim keys
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Resize panes with vim keys (prefix + H/J/K/L)
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5
