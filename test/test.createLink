#!/usr/bin/env bash
#clear
#export PS4='\033[90m+${LINENO} in ${#BASH_SOURCE[@]}>${FUNCNAME[0]}:${BASH_SOURCE[@]##*/} \033[0m'
#set -x

level=$1
if [ -z "$level" ]; then
  level=1
fi
echo "starting: ${BASH_SOURCE[@]##*/} <LOG_LEVEL=$1>"

#echo "sourcing init"
source this
source test.suite

log.level $level

competionArray=(once config list file ite)
source oo

test.createLink() 
{
((TEST_COUNTER++))
console.log "


Test 0: createLink \"$*\"
===================================================================="
createLink.start "$@"
console.log "RETURN: $RETURN_VALUE  Result: $RESULT 
===================================================================="
}

test.case - "createLink test start" \
   createLink $*
expect 0 "*" "Test start"

source createLink


console.log "
Test 1 
===================================================================="

test.case - "Test if simbolic link is being created for the respective user" \
   createLink.user /var/dev/EAMD.ucp snet
expect 0 "0"

### test.method

