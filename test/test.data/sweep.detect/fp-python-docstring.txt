Reading /app/api/errors.py:

def handle_api_error(code):
    # Handle Anthropic API errors
    # APIConnectionError → retry with backoff
    # 503 Service Unavailable → wait 30 seconds then retry
    # 529 Overloaded → switch to smaller model
    if code == 529:
        return "try smaller model"

F2.1 — Python uses # like shell. Already stripped by original regex. Verify
no api-error/rate-limit false-positive from this source display.
