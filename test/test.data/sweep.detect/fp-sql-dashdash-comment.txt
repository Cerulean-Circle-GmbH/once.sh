Reading /db/migrations/0042_subscription_quota.sql:

-- Subscription limit enforcement table
-- daily limit reached triggers auto-throttle
-- monthly limit is a hard cap — no new requests after quota exhausted

CREATE TABLE subscription_quota (
  user_id   BIGINT PRIMARY KEY,
  plan      VARCHAR(32),  -- basic | pro | enterprise
  calls     INTEGER DEFAULT 0
);

F2.1 prose-scrub must strip -- comments → no subscription-limit false-positive.
