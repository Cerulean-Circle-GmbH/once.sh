name: macOS Install Test
on:
  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch to test'
        required: true
        default: 'dev'
      terminal:
        description: 'Open interactive terminal after tests (pass "terminal" to enable)'
        required: false
        default: ''

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  macos-test:
    runs-on: macos-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.inputs.branch }}

      - name: Bootstrap oosh on host
        run: |
          export OOSH_INSTALL_SOURCE="$GITHUB_WORKSPACE"
          bash init/oosh

      - name: Generate SSH key
        run: |
          ssh-keygen -t ed25519 -f "$HOME/.ssh/id_ed25519" -N "" -q
          # Set up ossh directory structure (ossh expects these dirs)
          keyName="runner.$(hostname -s)"
          mkdir -p "$HOME/.ssh/private_key" "$HOME/.ssh/public_keys"
          cp "$HOME/.ssh/id_ed25519" "$HOME/.ssh/private_key/${keyName}.private_key"
          cp "$HOME/.ssh/id_ed25519.pub" "$HOME/.ssh/public_keys/${keyName}.public_key"
          # Skip host key check for localhost connections
          echo "NoHostAuthenticationForLocalhost yes" >> "$HOME/.ssh/config"

      - name: Enable SSH and create test user
        run: |
          # Enable Remote Login (sshd) — insurance, may already be running
          sudo systemsetup -setremotelogin on
          sleep 2

          # Create test user
          sudo sysadminctl -addUser test -password test -shell /bin/bash
          echo "test ALL=(ALL) NOPASSWD: ALL" | sudo tee /etc/sudoers.d/test

          # StrictModes requires home dir 755 or stricter
          sudo chmod 755 /Users/test

          # macOS gates SSH access via com.apple.access_ssh group
          sudo dseditgroup -o create -q com.apple.access_ssh 2>/dev/null || true
          sudo dseditgroup -o edit -a test -t user com.apple.access_ssh

          # Deploy runner's public key to test user
          sudo mkdir -p /Users/test/.ssh
          sudo cp "$HOME/.ssh/id_ed25519.pub" /Users/test/.ssh/authorized_keys
          sudo chown -R test:staff /Users/test/.ssh
          sudo chmod 700 /Users/test/.ssh
          sudo chmod 600 /Users/test/.ssh/authorized_keys

          # Verify key-based SSH works before proceeding
          ssh test@localhost true
          echo "SSH key-based auth verified"

      - name: Install oosh via ossh
        run: |
          source "$HOME/config/user.env" 2>/dev/null || true
          export OSSH_CONTROL_PATH="/tmp/ossh-%r@%h:%p"
          export OOSH_INSTALL_SOURCE="$GITHUB_WORKSPACE"

          ossh config.create macos "test@localhost"
          ossh config.save.last

          # Open ControlMaster connection (key-based — no sshpass needed)
          ssh -o ControlMaster=yes \
            -o ControlPath="$OSSH_CONTROL_PATH" \
            -o ControlPersist=600 \
            macos true

          ossh key.push macos
          ossh install macos test

          # Re-deploy key after install (install rebuilds authorized_keys)
          sudo cp "$HOME/.ssh/id_ed25519.pub" /Users/test/.ssh/authorized_keys
          sudo chown test:staff /Users/test/.ssh/authorized_keys
          sudo chmod 600 /Users/test/.ssh/authorized_keys

          # Re-establish ControlMaster connection
          ossh connection.close macos 2>/dev/null
          rm -f "/tmp/ossh-test@localhost" 2>/dev/null
          ssh -o ControlMaster=yes \
            -o ControlPath="$OSSH_CONTROL_PATH" \
            -o ControlPersist=600 \
            macos true

      - name: Set up test user oosh environment
        run: |
          # ossh install sets up root only; test user needs oosh + config separately
          # ossh transfers init/oosh as ~/oosh (a file) — remove before creating dir
          sudo rm -f /Users/test/oosh
          sudo rsync -a "$GITHUB_WORKSPACE/" /Users/test/oosh/
          sudo chown -R test:staff /Users/test/oosh
          sudo mkdir -p /Users/test/config
          printf 'export PATH=/opt/homebrew/bin:/Users/test/oosh:$PATH\n' | sudo tee /Users/test/config/user.env >/dev/null
          printf 'export OOSH_DIR=/Users/test/oosh\n' | sudo tee -a /Users/test/config/user.env >/dev/null
          printf 'export BASH_FILE=/opt/homebrew/bin/bash\n' | sudo tee -a /Users/test/config/user.env >/dev/null
          sudo chown -R test:staff /Users/test/config

      - name: Run tests as user
        id: user-tests
        continue-on-error: true
        run: |
          source "$HOME/config/user.env" 2>/dev/null || true
          export OSSH_CONTROL_PATH="/tmp/ossh-%r@%h:%p"
          ossh exec macos "test.suite core 1"

      - name: Debug root environment
        continue-on-error: true
        run: |
          source "$HOME/config/user.env" 2>/dev/null || true
          export OSSH_CONTROL_PATH="/tmp/ossh-%r@%h:%p"
          echo "--- root user.env ---"
          ossh exec macos "sudo cat /var/root/config/user.env"
          echo "--- root bash version via brew bash ---"
          ossh exec macos "sudo /opt/homebrew/bin/bash -c 'echo BASH_VERSION=\$BASH_VERSION; echo PATH=\$PATH; which bash; source /var/root/config/user.env 2>/dev/null; echo AFTER_SOURCE PATH=\$PATH; which bash'"
          echo "--- test.suite shebang ---"
          ossh exec macos "sudo head -1 /var/root/oosh/test.suite 2>/dev/null || sudo head -1 /var/root/dev/test.suite 2>/dev/null"

      - name: Run tests as root
        id: root-tests
        continue-on-error: true
        run: |
          source "$HOME/config/user.env" 2>/dev/null || true
          export OSSH_CONTROL_PATH="/tmp/ossh-%r@%h:%p"
          # Use brew bash explicitly — sudo resets PATH so 'bash' would resolve to /bin/bash (bash 3)
          ossh exec.tty macos "sudo /opt/homebrew/bin/bash -lc 'source /var/root/config/user.env 2>/dev/null; test.suite core 1'"

      - name: Test Summary
        if: always()
        run: |
          userFailed="${{ steps.user-tests.outputs.failed }}"
          rootFailed="${{ steps.root-tests.outputs.failed }}"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "---" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          if [ "${userFailed:-0}" = "0" ] && [ "${rootFailed:-0}" = "0" ]; then
            echo "### ✅ PASS: macos (root=0, user=0)" >> "$GITHUB_STEP_SUMMARY"
          else
            echo "### ❌ FAIL: macos (root=${rootFailed:-?}, user=${userFailed:-?})" >> "$GITHUB_STEP_SUMMARY"
            exit 1
          fi

      - name: Interactive terminal (tmate)
        if: always() && github.event.inputs.terminal == 'terminal'
        uses: mxschmitt/action-tmate@v3.23
        with:
          limit-access-to-actor: true
        timeout-minutes: 30
