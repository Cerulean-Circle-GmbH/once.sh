#!/usr/bin/env bash
#clear
#export PS4='\e[90m+${LINENO} in ${#BASH_SOURCE[@]}>${FUNCNAME[0]}:${BASH_SOURCE[@]##*/} \e[0m'
#set -x

#echo "starting: $0 <LOG_LEVEL=$1>"

# Ensure logging stubs exist — config may be sourced before log during bootstrap
[ "$(type -t console.log)" = "function" ] || console.log() { :; }
[ "$(type -t important.log)" = "function" ] || important.log() { :; }
[ "$(type -t warn.log)" = "function" ] || warn.log() { :; }
[ "$(type -t stop.log)" = "function" ] || stop.log() { :; }
[ "$(type -t debug.log)" = "function" ] || debug.log() { :; }
[ "$(type -t error.log)" = "function" ] || error.log() { echo "ERROR: $*" >&2; }

config.completion.discover() {
  #set -x
  #info.log "completion.discover $*"
  local command=$1
  shift
  local ac=$(command -v $command)
  case "${command}" in
    "init"|"oosh"|"this")
      command="this"
      ac=$OOSH_DIR/this
    ;;
    "oo")
      if [ -n "$1" ]; then
        #set -x
        command=$1
        shift
        ac=$(command -v $command)
        if [ -n "$ac" ]; then
          config.completion.discover $ac
          return 0
        else
          cd $OOSH_DIR
          compgen -f $command
          return 0
        fi
      fi
    ;;    
    "once.sh")
      command="once"
    ;;
    "config")
      debug.log "in config"
      ac=$OOSH_DIR/config
    ;;
    *)
      debug.log "$command not found in completion"
    ;;
  esac
  
  local method=""
  local previous=""
  local last=""
  while [ -n "$1" ]; do
   previous=$method
   if [ -z "$method" ]; then
    method=$1
   else
    method=$method.$1
   fi
   last=$1
   shift
  done

  # if [ -n "$command" ]; then
  #   if [ -x "$(command -v $command)" ]; then
  #     info.log "sourcing $command"
  #     #source "$command"
  #   else
  #     return 1
  #   fi
  # else
  #   return 1
  # fi


  if (config.check.completion $command $previous); then
      $ac $previous.completion $last $command $previous
  fi

  if (config.check.completion $command $last); then
    if [ "$ac" = "$OOSH_DIR/config" ]; then
      if (this.functionExists $command.$last.completion); then
        $command.$last.completion '' $command $previous
        return 0
      fi
      if (this.functionExists $command.$previous.completion); then
        $command.$previous.completion $last $command $previous
        return 0
      fi
    else

      $ac $last.completion '' $command $previous
      return 0
    fi
  fi
  # if (this.functionExists $command.$previous.completion); then
  #   $command.$previous.completion $last $command $previous
  #   return 0
  # fi

  if [[ $method == *[\\/\(\)]* ]]; then
    create.result 1 "$method contains special characters"
    #echo $RESULT
    return "$(result)" 
  fi

  #echo method:$method
  local find="^\(function \)*\($command\.\)\($method[^(#\"]*\)\(.*\)"

  grep "$find" $ac | sed 's/'"$find"'/\3/' | sed 's/\(.*\)\(\.completion.*\)/\1/'
  #echo "$@"
}

config.check.completion() {
  local command=$1
  local ac=$(command -v $command)
  local method="$2.completion"
  local find="^\(function \)*\($command\.\)\($method[^(#\"]*\)\(.*\)"
  if [[ $method == *[\\/\(\)]* ]]; then
    create.result 1 "$method contains special characters"
    #echo $RESULT
    return "$(result)"
  fi
  RESULT=$(grep "$find" $ac | sed 's/'"$find"'/\3/')
  if [ "$RESULT" = "$method" ]; then
    create.result 0 "$RESULT"
  else
    create.result 1 "$method not found"
  fi
  debug.log "$RESULT"
  return "$(result)"
}


config.edit.completion() {
  config.list.completion "$@"
}

config.delete.completion() {
  config.list.completion "$@"
}



config.list.completion.name() {
  #echo "$@"
  export FILTER=$1
  # Offer names from BOTH config tiers: shared $CONFIG_PATH (log, oosh, user) and
  # per-user $OOSH_USER_CONFIG_PATH (log.session). Guard each glob so an
  # unmatched pattern isn't emitted literally.
  for file in ${CONFIG_PATH}/*.env "${OOSH_USER_CONFIG_PATH:-$HOME/.config/oosh}"/*.env
    do
      [ -e "$file" ] || continue
      echo "$(basename $file)" | line replace ".env"
    done
}

config.string.quote() { # <string> # quote the string to be used as a command line argument
  RESULT=$(printf %s\\n "$1" | sed "s/'/'\\\\''/g;1s/^/'/;\$s/\$/'/")
  echo $RESULT
}

config.init() # # inititalizes the $CONFIG environment if $CONFIG is empty
{
  # HONOUR an anchor already set; default to the constant when there is none.
  # This used to be an unconditional `export CONFIG_PATH=~/config`, and
  # config.start re-runs config.init whenever $CONFIG is not a file — so
  # sourcing `config` silently moved the anchor back and undid any fixture
  # FROM INSIDE the file under test. That is why test/test.config and
  # test/test.completion.audit had to declare TEST_SHARED_TIER_WRITER instead
  # of isolating.
  #
  # The conditional form is not a bend of the anchor rule, it IS the rule:
  # private.this.anchor.validate.one counts `: ${CONFIG_PATH:=~/config}` as
  # CONFORMING, and `this`, `log` and `ossh` already use exactly this shape for
  # exactly this variable. A cold shell still lands on the constant.
  # CONFIG_PATH only. CONFIG_FILE is NOT an anchor — it is the currently
  # selected file, and `config file reset` means "go back to user.env" and
  # reaches that through here (config.file's reset branch). Making it
  # conditional too broke T16, which is exactly the assertion that pins it.
  : ${CONFIG_PATH:=~/config}
  export CONFIG_PATH
  export CONFIG_FILE=user.env

  if [ ! -d $CONFIG_PATH ]; then
    mkdir $CONFIG_PATH
  #else
  #  source $CONFIG
  fi
  touch $CONFIG_PATH/error.txt

  export CONFIG=$CONFIG_PATH/$CONFIG_FILE
  debug.log "$CONFIG"
}

# ---------------------------------------------------------------------------
# Repair primitives — bring a tampered/corrupted OOSH layout back to the
# canonical install state (matches what init/oosh produces). NOT called by
# the bashrc-startup `config.init` above; that path stays cheap.
# Reference state: ~/config and ~/oosh symlinks owned <user>:<user>;
# sharedConfig dir mode 2775 group dev; no self-referential symlinks.
# ---------------------------------------------------------------------------

config.init.shared() # # ensure sharedConfig dir is mode 2775 group dev; remove self-loops; idempotent
{
  # `user` script provides user.get, private.as.user, user.group.add — not
  # auto-loaded by `source this`. Source on demand so `./config init.*` works
  # in standalone invocations (the c2 dispatch path in interactive shells
  # already pulls it in).
  [ "$(type -t user.get)" = "function" ] || source "$OOSH_DIR/user" >/dev/null 2>&1
  local devHome
  devHome=$(user.get home developking 2>/dev/null)
  if [ -z "$devHome" ]; then
    error.log "config.init.shared: cannot resolve developking's home — run ${YELLOW}ossh install${NORMAL} first"
    return 1
  fi
  local baseDir
  baseDir=$(dirname "$devHome")
  local sharedConfig="$baseDir/shared/EAMD.ucp/Scenarios/localhost/EAM/1_infrastructure/Once.sh/sharedConfig"
  if [ ! -d "$sharedConfig" ]; then
    error.log "config.init.shared: sharedConfig missing: ${RED}$sharedConfig${NORMAL} — install not completed"
    return 1
  fi
  important.log "config.init.shared: normalising ${YELLOW}$sharedConfig${NORMAL} (mode 2775, group dev)"
  private.ensure.sharedTree "$sharedConfig"
}

config.init.user() # <username:$USER> # ensure the user's ~/config + ~/oosh symlinks match canonical install
{
  [ "$(type -t user.get)" = "function" ] || source "$OOSH_DIR/user" >/dev/null 2>&1
  local username="$1"
  if [ -n "$1" ]; then shift; fi
  if [ -z "$username" ]; then username="$USER"; fi

  local targetHome
  targetHome=$(user.get home "$username" 2>/dev/null)
  if [ -z "$targetHome" ]; then
    error.log "config.init.user: cannot resolve home for ${YELLOW}$username${NORMAL}"
    return 1
  fi
  local devHome
  devHome=$(user.get home developking 2>/dev/null)
  if [ -z "$devHome" ]; then
    error.log "config.init.user: developking missing — run ${YELLOW}ossh install${NORMAL} first"
    return 1
  fi
  local baseDir
  baseDir=$(dirname "$devHome")
  local sharedConfig="$baseDir/shared/EAMD.ucp/Scenarios/localhost/EAM/1_infrastructure/Once.sh/sharedConfig"
  # Resolve the canonical shared oosh dir. Preference order, picking the
  # first that resolves to an existing dir under the shared tree:
  #   1. The current $OOSH_DIR (we're running from it — trustworthy).
  #   2. The existing target of <user>'s ~/oosh symlink (if already aligned).
  #   3. The branch named by $OOSH_MODE / git fallback / "dev".
  # Avoids the case where $OOSH_MODE is set to a stale value like "released"
  # that doesn't have a corresponding worktree on disk.
  local sharedOoshBase="$baseDir/shared/EAMD.ucp/Components/com/ceruleanCircle/EAM/1_infrastructure/Once.sh"
  local sharedOosh=""
  # OOSH_DIR is ~/oosh (a symlink), so the prefix test needs its PHYSICAL
  # target — the literal never starts with $sharedOoshBase. See docs/boot.md.
  local ooshPhysical
  ooshPhysical=$(private.this.path.canonical "$OOSH_DIR" 2>/dev/null || echo "$OOSH_DIR")
  if [ -d "$ooshPhysical" ] && [ "${ooshPhysical#$sharedOoshBase/}" != "$ooshPhysical" ]; then
    sharedOosh="$ooshPhysical"
  elif [ -L "$targetHome/oosh" ]; then
    local _existing
    _existing=$(readlink "$targetHome/oosh")
    if [ -d "$_existing" ] && [ "${_existing#$sharedOoshBase/}" != "$_existing" ]; then
      sharedOosh="$_existing"
    fi
  fi
  if [ -z "$sharedOosh" ]; then
    local branch="$OOSH_MODE"
    [ -z "$branch" ] && branch=$(this.git.branch.short "$OOSH_DIR" 2>/dev/null)
    [ -z "$branch" ] && branch="dev"
    sharedOosh="$sharedOoshBase/$branch"
  fi
  if [ ! -d "$sharedOosh" ]; then
    error.log "config.init.user: ${RED}$sharedOosh${NORMAL} missing — install not completed"
    return 1
  fi

  important.log "config.init.user: aligning ${YELLOW}$username${NORMAL}'s ~/config and ~/oosh to canonical state"

  # Symlink reconciliation in two phases.
  #
  # Phase 1 — recreate (only if needed): dispatch through private.as.user so
  # newly-created symlinks inherit <user>:<user> ownership naturally. This is
  # the same trick user.oosh.install uses at user:787-794 — and the reason
  # fresh installs produce hannesn:hannesn while a tampered install path can
  # leave hannesn:dev. Skip the dispatch entirely when both symlinks already
  # point at the canonical targets — avoids the runuser PAM-session overhead
  # and an observed silent-failure mode under sudo when env is sanitised.
  #
  # Phase 2 — force ownership: chown -h is run by THIS shell (not via
  # private.as.user). When the caller is root (the typical sudo path) chown
  # works for any target. When the caller is the target user, chown to their
  # own primary group works without sudo. Other combinations are skipped
  # quietly (e.g. sudoer alice repairing bob — the recreate path already
  # gave bob:bob).
  local _ts
  _ts=$(date +%Y%m%d-%H%M%S)
  local _needsRecreate=0
  if [ ! -L "$targetHome/config" ] || [ "$(readlink "$targetHome/config")" != "$sharedConfig" ] \
  || [ ! -L "$targetHome/oosh"   ] || [ "$(readlink "$targetHome/oosh")"   != "$sharedOosh"   ] \
  || [ -d "$targetHome/config" -a ! -L "$targetHome/config" ] \
  || [ -d "$targetHome/oosh"   -a ! -L "$targetHome/oosh"   ]; then
    _needsRecreate=1
  fi
  if [ "$_needsRecreate" = "1" ]; then
    # Delegate symlink convergence to the shared primitive
    # private.this.symlink.with.backup (this:171). private.as.user runs
    # the operations AS the target user, so the symlinks naturally inherit
    # <user>:<user> ownership — the canonical install layout.
    #
    # The bash -c subshell sources $OOSH_DIR/this to bring the primitive
    # (and its create.result / error.log / result dependencies) into
    # scope. We deliberately DON'T `set -e` here: `this` returns non-zero
    # at load-time as part of its dispatch protocol (it's a callable
    # script as well as a sourceable library), so `set -e` + source would
    # exit the subshell before the primitive ever ran. Explicit
    # `|| exit 1` after each primitive call gives the same fail-fast
    # behaviour without the false-positive on the source.
    private.as.user "$username" bash -c "
      cd \"$targetHome\" || exit 1
      source \"$OOSH_DIR/this\" >/dev/null 2>&1
      private.this.symlink.with.backup config '$sharedConfig' '$_ts' || exit 1
      private.this.symlink.with.backup oosh   '$sharedOosh'   '$_ts' || exit 1
    " || {
      error.log "config.init.user: failed to create symlinks for ${RED}$username${NORMAL}"
      return 1
    }
  fi
  # Phase 2 — force canonical owner on the symlinks. Only when this shell has
  # the privilege to do so (root, or self).
  if [ "$(id -u)" = "0" ] || [ "$username" = "$USER" ]; then
    chown -h "$username:$username" "$targetHome/config" "$targetHome/oosh" 2>/dev/null || true
  fi

  # bashrc — install template if the OOSH section is missing (idempotent).
  # Same one-shot .bashrc.pre-oosh backup convention as user:850.
  private.as.user "$username" bash -c "
    [ -f \"$targetHome/.bashrc\" ] && [ ! -f \"$targetHome/.bashrc.pre-oosh\" ] && cp \"$targetHome/.bashrc\" \"$targetHome/.bashrc.pre-oosh\"
    if ! grep -q 'oosh/boot' \"$targetHome/.bashrc\" 2>/dev/null; then
      if [ -f \"$sharedOosh/templates/user/bashrcTemplate\" ]; then
        cp \"$sharedOosh/templates/user/bashrcTemplate\" \"$targetHome/.bashrc\"
      else
        printf '\n# oosh environment\n[ -f ~/oosh/boot ] && source ~/oosh/boot\n' >> \"$targetHome/.bashrc\"
      fi
    fi
    [ ! -f \"$targetHome/.bash_profile\" ] && echo '[ -f ~/.bashrc ] && source ~/.bashrc' > \"$targetHome/.bash_profile\"
  " 2>/dev/null

  # Ensure dev group membership (idempotent; user.group.add no-ops if already a member).
  if private.user.check.group dev >/dev/null 2>&1; then
    user.group.add dev "$username" >/dev/null 2>&1
  fi

  success.log "config.init.user: ${GREEN}$username${NORMAL} aligned"
}

config.init.check() # <username:$USER> # diagnostics: report symlink state, dev-group health (no fixes)
{
  [ "$(type -t user.get)" = "function" ] || source "$OOSH_DIR/user" >/dev/null 2>&1
  local username="$1"
  if [ -n "$1" ]; then shift; fi
  if [ -z "$username" ]; then username="$USER"; fi

  local targetHome
  targetHome=$(user.get home "$username" 2>/dev/null)
  if [ -z "$targetHome" ]; then
    error.log "config.init.check: cannot resolve home for ${YELLOW}$username${NORMAL}"
    return 0  # diagnostic — never block
  fi

  important.log "config.init.check: ${YELLOW}$username${NORMAL} (home: $targetHome)"

  # ~/config — owner, target, target's mode/group
  if [ -L "$targetHome/config" ]; then
    local cOwner cTarget tMode tGroup
    cOwner=$(stat -c "%U:%G" "$targetHome/config" 2>/dev/null || stat -f "%Su:%Sg" "$targetHome/config" 2>/dev/null)
    cTarget=$(readlink "$targetHome/config")
    info.log "  ~/config: owner=$cOwner target=$cTarget"
    if [ -d "$cTarget" ]; then
      tMode=$(stat -c "%a" "$cTarget" 2>/dev/null || stat -f "%Lp" "$cTarget" 2>/dev/null)
      tGroup=$(stat -c "%G" "$cTarget" 2>/dev/null || stat -f "%Sg" "$cTarget" 2>/dev/null)
      info.log "  sharedConfig: mode=$tMode group=$tGroup"
      if [ "$tGroup" != "dev" ]; then
        warn.log "  sharedConfig group ${RED}$tGroup${NORMAL} drifted from canonical ${GREEN}dev${NORMAL} — run ${YELLOW}config init.shared${NORMAL}"
      fi
      if [ -L "$cTarget/sharedConfig" ]; then
        warn.log "  self-referential symlink present at $cTarget/sharedConfig — run ${YELLOW}config init.shared${NORMAL}"
      fi
    fi
    # Symlink owner should be <user>:<user>, not <user>:dev
    case "$cOwner" in
      "$username:$username") : ;;
      *) warn.log "  ~/config owner ${RED}$cOwner${NORMAL} drifted from canonical ${GREEN}$username:$username${NORMAL} — run ${YELLOW}config init.user${NORMAL}" ;;
    esac
  else
    warn.log "  ~/config is not a symlink — run ${YELLOW}config init.user${NORMAL}"
  fi

  # Stale-dev-group warning: in /etc/group but not in active group set.
  # We can only check the *running* shell's group set when the username is
  # the caller; for other users we just check static membership.
  if getent group dev 2>/dev/null | grep -qE "(:|,)$username(,|$)"; then
    if [ "$username" = "$USER" ]; then
      if ! id -nG | grep -qw dev; then
        important.log "${YELLOW}$username${NORMAL} is in group ${GREEN}dev${NORMAL} per /etc/group, but the current shell's active groups don't include it. Log out of the desktop session ${RED}fully${NORMAL} (not just the terminal) and log back in — the active group set is captured at login time. Until then, writes to ~/config/* will hit Permission denied."
      fi
    fi
  else
    warn.log "  ${YELLOW}$username${NORMAL} not in group dev — run ${YELLOW}config init.user${NORMAL} to add"
  fi

  # Required-variable and branch-drift report. Diagnostic ONLY: the rc is
  # swallowed on purpose, because this function is documented as never failing
  # its caller and T34 pins that. The rc-1 channel is the user-invoked
  # `config validate required`, which is what a script should branch on.
  if [ "$username" = "$USER" ]; then
    config.validate.required || :
  fi
  return 0
}

config.init.env() # # regenerate user.env + oosh.env + log.env via config.save (mirrors install at oo:1456)
{
  if [ -z "$OOSH_DIR" ]; then
    error.log "config.init.env: \$OOSH_DIR is empty — re-source ${YELLOW}this${NORMAL} or rerun under ${YELLOW}sudo -E${NORMAL}"
    return 1
  fi
  # CONFIG_PATH must point at the shared sharedConfig dir we want to write
  # into. Trust the calling context: bashrc/this set it to ~/config (the
  # symlink), which `config init.user` ensures points at sharedConfig.
  if [ -z "$CONFIG_PATH" ] || [ ! -d "$CONFIG_PATH" ]; then
    error.log "config.init.env: \$CONFIG_PATH ${RED}$CONFIG_PATH${NORMAL} missing — run ${YELLOW}config init.user${NORMAL} first"
    return 1
  fi
  # Back up ALL THREE, not just user.env. Only user.env was copied before, and
  # that is exactly how OOSH_PM was lost on the macOS VM: `config save oosh
  # OOSH` truncates oosh.env with `{ … } >$CONFIG` before anything can read the
  # surviving value out of it, so by the time the save fails there is nothing
  # left to restore from. The refuse-to-worsen guard below restores from these.
  local _ts _envName
  _ts=$(date +%Y%m%d-%H%M%S)
  for _envName in user oosh log; do
    [ -f "$CONFIG_PATH/$_envName.env" ] || continue
    cp -p "$CONFIG_PATH/$_envName.env" "$CONFIG_PATH/$_envName.env.bak.$_ts" 2>/dev/null \
      || warn.log "config.init.env: failed to back up $_envName.env (continuing)"
  done
  important.log "config.init.env: backed up user/oosh/log .env → ${GREEN}*.env.bak.$_ts${NORMAL}"

  # WHAT WAS MISSING BEFORE THIS TICKET. config.save persists the ENVIRONMENT,
  # and the environment is populated FROM the config — a closed loop with no
  # source of truth. So regenerating a config that had lost a variable simply
  # wrote the gap back, and on the macOS VM the report went from two missing
  # variables to three with oosh.env emptied.
  #
  # Derive here, in the REPAIR, and never in config.save: `oo pm.discover`
  # reaches `config save` internally (oo:3045 → config's log.device HACK → a
  # recursive `config save log LOG`), and that cascade is what wrote a shared
  # config tier on macOS. The writer stays a pure persister.
  #
  # Per-variable emptiness guard, exactly as private.user.init's OS_CMD heal
  # does it (user:1655-1699): a value that survived is NEVER overwritten by a
  # derived one. The verdicts go to stdout — nothing captures config.init.env,
  # unlike config.save — so an operator running the advertised repair can see
  # what was reconstructed and what could not be.
  local _variable _file _deriver _derived _repaired="" _unrecoverable=""
  while read -r _variable _file _deriver; do
    [ -z "$_variable" ] && continue
    [ -n "${!_variable}" ] && continue          # it survived; leave it alone
    [ "$(type -t "$_deriver")" = "function" ] || { _unrecoverable="$_unrecoverable $_variable"; continue; }
    _derived=$("$_deriver" 2>/dev/null)
    if [ -n "$_derived" ]; then
      export "$_variable=$_derived"
      _repaired="$_repaired $_variable"
    else
      _unrecoverable="$_unrecoverable $_variable"
    fi
  done <<REQUIRED
$(private.config.required.variables.get)
REQUIRED
  [ -n "$_repaired" ]      && echo "config.init.env: re-derived:$_repaired"
  [ -n "$_unrecoverable" ] && echo "config.init.env: could NOT derive:$_unrecoverable — the regenerated config will still be missing these"

  important.log "config.init.env: regenerating ${YELLOW}user.env${NORMAL} + ${YELLOW}oosh.env${NORMAL} + ${YELLOW}log.env${NORMAL} via ${YELLOW}config save${NORMAL} (mirrors install)"
  # `config save` (no args) cascades internally: writes user.env, then calls
  # itself with (oosh OOSH) and (log LOG).
  config.save || warn.log "config.init.env: config.save returned non-zero — env files may be partial"

  # REFUSE TO WORSEN. The command `config validate required` advertises as the
  # repair must never again be able to make its own report worse — which is
  # precisely what it did on the VM. Count what is missing afterwards; if the
  # tier came out with FEWER export lines than it went in with, put the backups
  # back and say so.
  local _envRc=0
  for _envName in user oosh log; do
    local _bak="$CONFIG_PATH/$_envName.env.bak.$_ts"
    [ -f "$_bak" ] || continue
    local _wasCount _nowCount
    _wasCount=$(grep -c '^[[:space:]]*export ' "$_bak" 2>/dev/null)
    _nowCount=$(grep -c '^[[:space:]]*export ' "$CONFIG_PATH/$_envName.env" 2>/dev/null)
    if [ "${_nowCount:-0}" -lt "${_wasCount:-0}" ]; then
      echo "config.init.env: REFUSING — $_envName.env went from ${_wasCount:-0} to ${_nowCount:-0} export lines; restoring $_bak"
      cp -p "$_bak" "$CONFIG_PATH/$_envName.env" 2>/dev/null
      _envRc=1
    fi
  done
  return $_envRc
}

config.init.full() # <username:$USER> # repair end-to-end: shared dir + user symlinks + env files + diagnostics
{
  local username="$1"
  if [ -n "$1" ]; then shift; fi
  if [ -z "$username" ]; then username="$USER"; fi

  local rc=0
  config.init.shared || rc=$?
  config.init.user "$username" || rc=$?
  # init.env runs AFTER init.user so the ~/config symlink is canonical
  # before we write into the target. Only run for self-repair — regenerating
  # other users' env files from the caller's shell would write THIS shell's
  # OOSH_*/LOG_* state into THEIR config (likely wrong values).
  if [ "$username" = "$USER" ] || [ "$(id -u)" = "0" -a "$username" = "root" ]; then
    config.init.env || rc=$?
  fi
  config.init.check "$username" || rc=$?
  return $rc
}

config.init.shared.completion() { :; }
config.init.env.completion()    { :; }
config.init.user.completion.username()  { user.list; }
config.init.check.completion.username() { user.list; }
config.init.full.completion.username()  { user.list; }

private.config.variables.list()     # <envPrefix> # echo one variable NAME per line for every set, scalar, persistable variable matching envPrefix #
{
  # NO create.result — a getter consumed as $(...), which runs in a subshell,
  # so RESULT could never reach the caller. Same reasoning as
  # private.config.required.variables.get and private.config.file.resolve.
  #
  # SILENT BY CONTRACT. The only caller runs inside `{ … } >$CONFIG`, and
  # log:35 sets LOG_DEVICE=/proc/self/fd/1 — inside that block fd 1 IS the
  # generated env file. One debug.log here would be written into it.
  #
  # compgen -v, NOT `declare -p` with no names. On bash 3.2 the no-argument
  # form behaves like bare `declare`: `set`-style output AND every function
  # body, verbatim and multi-line, in the same stream. That is how rows of
  # private.config.required.variables.get's heredoc became lines in
  # ~/config/oosh.env — docs/research/2026-09-17-config-save-bash32.md § 2.
  # `compgen -v` emits variable NAMES ONLY, on 3.2 and 5 alike (measured).
  #
  # ${1%_}: `backup config.save global` passes BACKUP_ (backup:262). With the
  # underscore already there the prefix gate read `BACKUP_|BACKUP__*`, which
  # BACKUP_TARGET matches neither, so backup.env was always written empty.
  local envPrefix="${1%_}"
  local variableName
  # These two locals are themselves listed by `compgen -v`, exactly as they
  # were by `declare -p`. camelCase cannot match an OOSH_/LOG_/CONFIG/OS_CMD_
  # prefix, so the harvest set is unchanged.
  # Read LINE BY LINE, never `for … in $(compgen -v)`. Unquoted command
  # substitution word-splits, which would SHRED a malformed row into its words
  # and let the first one through as if it were a name — the shape gate would
  # then be passing by accident. A line is the unit being validated.
  compgen -v 2>/dev/null | while IFS= read -r variableName; do
    # SHAPE GATE. The old filter derived a name with three string-strips
    # (`${_line#declare }`, `${_vn#-* }`, `${_vn%%=*}`) and never checked the
    # result was a name at all: a line with no `=` survived the last strip
    # whole, and a `case` glob spans spaces, so the row
    # `OOSH_MODE oosh.env basename of the canonical ~/oosh` matched `OOSH_*`
    # and was written out as if it were a variable.
    case "$variableName" in
      [A-Za-z_]*) ;;
      *) continue ;;
    esac
    case "$variableName" in *[!A-Za-z0-9_]*) continue ;; esac
    # Prefix as a whole name or a `name_…` family — never `${envPrefix}*`,
    # which for LOG would also grab LOGNAME.
    case "$variableName" in ${envPrefix}|${envPrefix}_*) ;; *) continue ;; esac
    case "$variableName" in
      # per-user / volatile / install-only / sudo-injected — never persist.
      #
      # Derived per-user paths are dynamically rebuilt from $HOME at every
      # shell init ($OOSH_DIR/boot and this.init set CONFIG_PATH / OOSH_DIR /
      # OOSH_USER_CONFIG_PATH), so persisting their ABSOLUTE expanded value
      # into shared config (the saving user's $HOME) only causes EACCES leaks
      # when a different user later sources it. Same reasoning as
      # LOG_LIVE/LOG_NAME/LOG_DEVICE (per-user, kept in
      # $OOSH_USER_CONFIG_PATH/log.session.env, not the shared log.env).
      # Install-only vars (LOG_INSTALL, INSTALL_LOG) must not persist into user
      # sessions. OOSH_COMPONENTS_DIR is a transient /tmp/test.oo.* path from
      # test runs — never share.
      #
      # OOSH_BRANCH is the install INPUT: the branch the OPERATOR ASKED FOR,
      # meaningful for the length of one install and meaningless afterwards. It
      # is not an absolute path, so it is here for the other reason this list
      # exists — state that must be DERIVED, never remembered. Persisting it
      # closed a loop (oosh.env seeds a shell, the shell reaches a config save,
      # the save writes it back) in which nothing ever consults the checkout,
      # and it left private.oo.install.branch.get answering "prod" on a dev box
      # for weeks. The branch a host is ON is derived from the canonical
      # ~/oosh; see private.oo.shared.base.get. That is T7.
      #
      # Documented at docs/config.md § Excluded variables and pinned by
      # test/test.config T29 — change all three together.
      *INSTALL*|LOG_NAME|LOG_LIVE|LOG_DEVICE|CONFIG_PATH|OOSH_USER_CONFIG_PATH|CONFIG|OOSH_DIR|OOSH_COMPONENTS_DIR|OOSH_BRANCH|SUDO_*|_) continue ;;
    esac
    echo "$variableName"
  done
}

private.config.variable.export.line()     # <variableName> # echo ONE export line for a set scalar variable; rc 1 for unset, array, or a value bash renders with ANSI-C quoting #
{
  # NO create.result — a getter consumed as $(...); it also runs in a loop and
  # would clobber the caller's RESULT on every iteration (config.save sets
  # RESULT="" before the block on purpose). SILENT BY CONTRACT, same fd-1
  # reason as private.config.variables.list.
  local variableName="$1"
  local declaration assignment
  # `declare -p` WITH AN EXPLICIT NAME — measured correct on bash 3.2.57
  # (`declare -- FOO="bar"`, `declare -a A='([0]="x")'`), unlike the
  # no-argument form. Bash does the quoting, so we hand-roll no escaper: the
  # same precedent, and the same reason, as result.into (this:719-728), whose
  # comment records what a bare `echo "$1=$value"` did to values with spaces.
  declaration=$(declare -p "$variableName" 2>/dev/null) || return 1
  case "$declaration" in
    'declare -a'*|'declare -A'*|'typeset -a'*|'typeset -A'*) return 1 ;;
  esac
  assignment="${declaration#declare -* }"
  # Output-side shape gate: refuse anything that is not `NAME=…` rather than
  # guess. `declare -- X` — declared, never assigned — lands here.
  case "$assignment" in "${variableName}="*) ;; *) return 1 ;; esac
  # TWO gates, and the first one is the one that matters.
  #
  # (1) THE VALUE. These files are LINE-ORIENTED — config.validate and
  # test.platform.shared.config.env.invariant both read them one line at a time
  # — so a value carrying a newline cannot be written at all, whatever quoting
  # bash chooses for it. Nothing in the OOSH_/LOG_/CONFIG_ families is designed
  # to hold one, so a value that does is an upstream bug, and
  # `config validate required` is the detector that names it as missing.
  #
  # Testing the RENDERING instead of the value was a real defect, caught by T77
  # on almalinux 9 during `oo stage testing` on 2026-09-18. `$'…'` is what bash
  # >= 5.2 emits; bash 5.1 emits a DOUBLE-QUOTED string with a literal newline
  # inside it — measured identically on almalinux 9 (5.1.8), ubuntu 22.04
  # (5.1.16) and debian 11 (5.1.4). So gate (2) below matched nothing on any of
  # them, and a two-line assignment was written into oosh.env. The comment that
  # used to sit here claimed ANSI-C quoting happened "on 3.2 as well as 5";
  # that was never measured and is false for every 5.1.
  local value="${!variableName}"
  case "$value" in *$'\n'*|*$'\r'*) return 1 ;; esac
  # (2) THE RENDERING, still worth keeping: if bash reaches for $'…' for any
  # other reason, that is a BASHISM, and $OOSH_DIR/boot sources these files
  # under dash/ash, which reads $'…' literally (review-2026-09-09 § D1).
  case "$assignment" in "${variableName}=\$'"*) return 1 ;; esac
  # PREPEND `export`. The old code sed-patched a `declare -` prefix into it,
  # which bash 3.2's no-argument form never prints — so on macOS the
  # substitution silently did nothing and no generated env file has ever
  # carried `export`.
  echo "export $assignment"
}

private.config.variables.export()     # <envPrefix> # echo the complete body of an env file: one export line per persistable variable matching envPrefix #
{
  # NO create.result — its stdout IS the file. SILENT BY CONTRACT.
  #
  # review-2026-09-09 § M3 named this before it existed: "config.save's
  # `declare -p | while read` harvesting loop is a 12-line inline block that
  # reads as private.config.vars.export <prefix>". Spelled with the full word
  # `variables` to match private.config.required.variables.get in this file.
  local envPrefix="$1"
  local variableName
  # Line by line, for the same reason as private.config.variables.list.
  private.config.variables.list "$envPrefix" | while IFS= read -r variableName; do
    private.config.variable.export.line "$variableName"
  done
}

private.config.string.upper()     # <string> # echo string upper-cased, without the bash-4 parameter expansion operator #
{
  # NO create.result — a getter consumed as $(...).
  #
  # `${string^^}` is bash 4+. On macOS /bin/bash 3.2 it is not a warning but a
  # FATAL "bad substitution" — the shell EXITS, measured on a sequoia VM. Both
  # config.save and config.add reached it from their DOCUMENTED single-argument
  # form (`config add oosh`, docs/config.md), so on macOS a documented command
  # killed the user's shell.
  #
  # printf | tr, not `tr <<< "$1"`: a here-string creates a temp file in
  # $TMPDIR, and this runs as root during install.
  printf '%s' "$1" | tr '[:lower:]' '[:upper:]'
}

# ── The required-variable derivers ───────────────────────────────────────────
# One per row of private.config.required.variables.get, whose third column now
# names the function instead of describing it in prose. Before this, that column
# was documentation of a repair that did not exist (F6), which is why
# config.init.env could only re-persist the gap it was asked to fix (F5).
#
# All six are GETTERS consumed as $(...), which runs in a subshell — so none
# calls create.result, the same exemption every other private.config.* getter
# carries. They DERIVE ONLY: none assigns, none persists, none writes a file.
# That separation is deliberate. `oo pm.discover` reaches `config save`
# internally (oo:3045-3053 → config:740's log.device HACK → a recursive
# `config save log LOG`), so derivation belongs in the REPAIR and never in the
# WRITER. See docs/research/2026-09-17-config-save-bash32.md § 2.

private.config.derive.oosh.mode()     # # the branch $HOME/oosh actually points at, its canonical basename #
{
  # The same two steps config.validate.required's drift check used inline
  # (and oo:2631-2633 a third time). Now one accessor, so the check and the
  # repair cannot disagree about what "the branch this host is on" means.
  # private.this.path.canonical, not `readlink -f` — the latter is GNU-only.
  local ooshTarget
  ooshTarget=$(private.this.path.canonical "$HOME/oosh" 2>/dev/null) || return 1
  [ -n "$ooshTarget" ] || return 1
  # A ~/oosh that is not there canonicalises to itself; that is not a branch.
  [ -d "$ooshTarget" ] || return 1
  echo "${ooshTarget##*/}"
}

private.config.derive.oosh.os()     # # the OS family for this host, via os.check.env #
{
  # os.check.env owns the $OSTYPE mapping and already sets OOSH_OS only when it
  # is empty, so calling it is safe whether or not the value is known. `config`
  # does not source `os`; the on-demand guard is the established route here
  # (config:215, config:235, oo:472).
  [ "$(type -t os.check.env)" = "function" ] || source "$OOSH_DIR/os" >/dev/null 2>&1
  [ "$(type -t os.check.env)" = "function" ] || return 1
  os.check.env >/dev/null 2>&1
  [ -n "$OOSH_OS" ] || return 1
  echo "$OOSH_OS"
}

private.config.derive.oosh.pm()     # # the package-manager install command for this host #
{
  # Already known? That IS the answer.
  [ -n "$OOSH_PM" ] && { echo "$OOSH_PM"; return 0; }
  # Second best: the binary survived even though the command did not.
  [ -n "$OOSH_PM_BIN" ] && { private.config.pm.command.get "$OOSH_PM_BIN"; return $?; }

  # NOT `oo pm.discover`. That verb is not a detector — it runs the platform's
  # package manager for real (`$SUDO apt-get update`, oo:3093-3104) and then
  # `config save`, so calling it from here hung a test run on a sudo password
  # prompt and would have re-entered the writer this card deliberately keeps
  # pure. test.suite:895-905 records the same trap being sprung once before.
  #
  # A DERIVER MUST HAVE NO SIDE EFFECTS. This is detection only: which package
  # manager exists, and what its install command is.
  local pmBin
  for pmBin in brew apt-get dnf yum apk pacman pkg; do
    command -v "$pmBin" >/dev/null 2>&1 || continue
    private.config.pm.command.get "$pmBin"
    return $?
  done
  return 1
}

private.config.pm.command.get()     # <packageManagerBinary> # the install command for a package-manager binary #
{
  # THE THIRD COPY of this table — init/oosh:214-222 has it in POSIX sh for the
  # bootstrap, and oo:3093-3101 has it as arguments to private.check.pm. A
  # fourth would be indefensible; even this one is only justified because the
  # other two are unreachable from here (init/oosh self-installs when sourced,
  # and oo's copy is fused to the installer that runs sudo). Carded: one
  # side-effect-free `oo pm.detect` that all three call.
  case "$1" in
    brew)    echo "brew install" ;;
    apt-get) echo "apt-get -y install" ;;
    dnf)     echo "dnf -y install" ;;
    yum)     echo "yum -y install" ;;
    apk)     echo "apk add" ;;
    pacman)  echo "pacman -S --noconfirm" ;;
    pkg)     echo "pkg install -y" ;;
    *)       return 1 ;;
  esac
}

private.config.derive.bash.file()     # # the bash interpreter this host should use #
{
  # What config.save has always done inline (and this:1190, this:1322). On
  # macOS `command -v bash` can answer /bin/bash 3.2 while brew bash 5 is the
  # one the install chose, so an already-set value wins — user:696-700 picks
  # the interpreter and it is not this function's business to second-guess it.
  [ -n "$BASH_FILE" ] && { echo "$BASH_FILE"; return 0; }
  local found
  found=$(command -v bash 2>/dev/null) || return 1
  [ -n "$found" ] || return 1
  echo "$found"
}

private.config.derive.config.file()     # # the name of the config file a shell starts from #
{
  # A constant, and deliberately so: config.init (config:188) and boot:77 both
  # hard-set it, and `config file reset` means "go back to user.env". It is a
  # row in the required table because a config without it is broken, not
  # because it is ever in doubt.
  echo "user.env"
}

private.config.derive.log.level()     # # the default log level #
{
  # An already-set level wins — a host that has chosen one keeps it. The
  # fallback is 3, which is what this:16 and log:125 both actually use. The
  # table used to say "defaults to 1" and no code anywhere agreed with it.
  [ -n "$LOG_LEVEL" ] && { echo "$LOG_LEVEL"; return 0; }
  echo "3"
}
### new.method

config.usage()
{
  local this=${0##*/}
  echo "You started" 
  echo "$0

  current file: $CONFIG
   CONFIG_PATH=$CONFIG_PATH
   CONFIG_FILE=$CONFIG_FILE


  Usage:
  $this: command   description and Parameter
      v         print version information
      init      initializes a new user config
      save      <?name> <?ENV_NAME> saves all vaiables containing ENV_NAME to name.env
      list      <?name> lists all vaiables in name.env
      add       <?name> lists all vaiables containing NAME to $CONFIG
      delete    <?name> deletes name.env
  
  Examples
    update current shell
      . \$CONFIG
      . $CONFIG

    $this v
    $this save once
    $this list oosh
  "
  
  #loop.list PATH print '---------------------: '
}

config.save() # <?name> <?ENV_PREFIX> # creates an "<name>.env" config file in $CONFIG_PATH containing all env variables that match <ENV_PREFIX>
# without parameters it inititlises the $CONFIG_PATH/user.env
{
  #set -x
  console.log "config.save (CONFIG=$CONFIG)"

  # config.save had no deliberate exit status at all — it fell off the end
  # through config.info.log, whose rc is a `[ … -gt … ]` test. It now reports
  # whether the files it wrote passed their own validation (see the guard at
  # the end of the no-arg branch).
  local configSaveRc=0

  local file=$1
  shift
  local name=$1
  if [ -n "$file" ] &&  [ -z "$name" ]; then
    #set -x
    # NOT `${file^^}` below. That is bash 4+, and on macOS /bin/bash 3.2 it is
    # not a warning but a FATAL "bad substitution" — the shell EXITS (measured
    # on a sequoia VM; the commands after it never ran). This branch is the
    # DOCUMENTED single-argument form — `config add oosh`, `config save myapp`
    # (docs/config.md) — so on macOS a documented command killed the user's
    # shell. The `warn.log "BASH 5 support only"` that stood here was never a
    # mitigation: the line under it had already ended the shell.
    name=$(private.config.string.upper "$file")
  else
    shift
  fi


  if [ -n "$file" ]; then
    config.file $file.env
  else
    name="CONFIG"
  fi
  
  stop.log "
  config.save [file:$file] [name:$name] to $CONFIG"
  # CARRY FORWARD what this shell has lost but the file still holds.
  #
  # `{ … } >$CONFIG` below TRUNCATES, and what it writes is whatever is in the
  # CURRENT ENVIRONMENT. So a shell missing a required variable rewrites the
  # file without it. `oo mode <branch>` is the live example (oo:938-940): it
  # sets OOSH_MODE and immediately saves oosh.env from that shell — which has
  # OOSH_MODE and, because nothing re-derives OOSH_OS at shell init, no
  # OOSH_OS. The file loses it. That is the oscillation measured on the macOS
  # VM: OOSH_OS present and OOSH_MODE missing at one point, the exact reverse
  # an hour later.
  #
  # This is NOT derivation, deliberately. Deriving belongs in config.init.env
  # (card 2), which can source `os` and afford to be slow. A writer only has to
  # refrain from throwing away what it was already holding — no new dependency,
  # no re-entrancy, and nothing to go wrong during an install.
  #
  # Read BEFORE the redirect: by the time `>$CONFIG` has run the old values are
  # gone, which is exactly how config.init.env lost OOSH_PM before card 2.
  local requiredVariable requiredFile requiredDeriver carriedValue
  while read -r requiredVariable requiredFile requiredDeriver; do
    [ -z "$requiredVariable" ] && continue
    [ "$requiredFile" = "$CONFIG_FILE" ] || continue   # only rows for the file being written
    [ -n "${!requiredVariable}" ] && continue          # the shell has it; it will be persisted
    carriedValue=$(grep -m1 -E "^[[:space:]]*(export[[:space:]]+)?${requiredVariable}=" "$CONFIG" 2>/dev/null \
                   | cut -d= -f2- | sed 's/^"//; s/"$//')
    [ -n "$carriedValue" ] && export "$requiredVariable=$carriedValue"
  done <<REQUIRED
$(private.config.required.variables.get)
REQUIRED

  RESULT=""
  {
    # The harvesting loop that used to live here is now
    # private.config.variables.export, and the exclusion list moved with it, to
    # sit beside the comment that explains it. review-2026-09-09 § M3 named the
    # extraction before it existed.
    #
    # It enumerates NAMES with `compgen -v` and renders each one with
    # `declare -p <name>`. It never parses the NO-ARGUMENT `declare -p` stream,
    # which on bash 3.2 is `set`-style AND contains every function body — the
    # two facts that between them wrote rows of the required-variables table
    # into ~/config/oosh.env on every macOS install.
    # See docs/research/2026-09-17-config-save-bash32.md.
    #
    # NOTHING inside this block may log. log:35 sets
    # LOG_DEVICE=/proc/self/fd/1, and inside this redirect fd 1 IS $CONFIG, so
    # a single debug.log would be written into the generated env file.
    private.config.variables.export "$name"
  } >$CONFIG
  private.ensure.groupWrite "$CONFIG"

  if [ -z "$file" ]; then
    #problem.log "regenerate config"

    important.log "generating $CONFIG"
    # user.env holds only PORTABLE config values (BASH_FILE = per-host bash;
    # CONFIG_FILE) followed by the source chain, so `config list` (cat user.env)
    # shows a meaningful config and sourcing user.env stands up the whole
    # environment. The per-user / volatile anchors (OOSH_DIR, CONFIG_PATH,
    # CONFIG, PATH) are deliberately NOT persisted — $OOSH_DIR/boot computes them
    # fresh at every shell init (correct per-user, robust to a moved config, no
    # cross-user leak). boot sets CONFIG_PATH BEFORE sourcing user.env, so the
    # appended `source $CONFIG_PATH/*.env` chain resolves. oosh.env / log.env
    # stay single-purpose (OOSH_* / LOG_*), no chaining of their own.
    [ -z "$BASH_FILE" ] && BASH_FILE=$(command -v bash)
    {
      echo "export BASH_FILE=\"$BASH_FILE\""
      echo "export CONFIG_FILE=\"$CONFIG_FILE\""
    } >$CONFIG


    ### HACK should be in log.init only. but is needed for ssh remote logging
      log.device $LOG_DEVICE
    ###


    config.save oosh OOSH   # pure `export OOSH_*` lines
    config.save log LOG     # pure `export LOG_*` lines
    # log.env chains the PER-USER session file (LOG_NAME/LOG_DEVICE/LOG_LIVE live
    # there — see $OOSH_USER_CONFIG_PATH). Written with the var UNEXPANDED (single
    # quotes), like user.env's `source $CONFIG_PATH/*.env`, so each user loads
    # their OWN file — no cross-user leak. This both makes `config list log` show
    # the session and LOADS the saved per-user values at shell init. boot
    # touch-guards the file so a first-ever shell has no missing-source error.
    # Appended here (not via config.add, which hardcodes $CONFIG_PATH).
    # POSIX `.` (not `source`) so boot parses under dash/ash — see config.add.
    echo '. $OOSH_USER_CONFIG_PATH/log.session.env' >> "$CONFIG_PATH/log.env"
    config.file user.env
    config.add oosh OOSH    # appends: source $CONFIG_PATH/oosh.env
    config.add log LOG      # appends: source $CONFIG_PATH/log.env

    # Guard: user.env may hold export/comment/blank + `source *.env` chaining
    # (composition, NOT logic); oosh.env / log.env must be pure. config.validate
    # allows the source lines but rejects real logic (self-anchors, if-blocks).
    #
    # THE VERDICT IS NO LONGER THROWN AWAY. This used to be
    # `config.validate … >/dev/null 2>&1 || warn.log …`, and that is why card
    # 1's corruption reached a user: config.validate caught it correctly on the
    # FIRST macOS install and the verdict went to /dev/null, with the only
    # signal a warn.log inside a 1 MB install log at a level that was not
    # printing. A guard whose verdict is discarded is not a guard.
    #
    # STDERR, not stdout, and the reason is load-bearing: user:1702-1708 records
    # that `$(user get basehome)` CAPTURES this function's stdout, and a stray
    # line there once propagated a garbage IdentityFile into a generated
    # 2cuGitHub config block and failed the downstream git clone. stderr
    # survives every LOG_LEVEL and LOG_DEVICE exactly as stdout would — which is
    # the whole point of the status-output idiom — and `$( )` does not capture
    # it. This is the one place in the tree where the idiom must use stderr, and
    # test/test.config T83 pins BOTH halves: the verdict on stderr, and stdout
    # staying clean.
    #
    # It REPORTS, it does not halt: config.save runs as root during install and
    # a non-zero rc here is already more than any caller acts on today (audited
    # 2026-09-17 — nothing branches on it; private.check.pm is the only place it
    # could propagate and oo.pm.discover masks it with `RETURN=$1`).
    local _envName _invalid=""
    for _envName in user oosh log; do
      config.validate "$_envName" >/dev/null 2>&1 || _invalid="$_invalid $_envName"
    done
    if [ -n "$_invalid" ]; then
      echo "INVALID: config.save wrote$_invalid with logic in it — run 'config validate <name>' to see why" >&2
      configSaveRc=1
    fi
    # if [ -x "$(command -v once)" ]; then
    #   once v
    # fi
  fi
  config.info.log
  return $configSaveRc
}

config.validate() # <?name:user> # verify an env file is PURE DATA (only export/comment/blank lines); rc 1 + report on any logic line. <name> is a bare name (user/oosh/log), a filename (x.env), or a path.
{
  local f="${1:-user}"
  case "$f" in
    */*)   : ;;                       # already a path
    *.env) f="$CONFIG_PATH/$f" ;;     # a filename in CONFIG_PATH
    *)     f="$CONFIG_PATH/$f.env" ;; # a bare name → CONFIG_PATH/<name>.env
  esac
  if [ ! -f "$f" ]; then
    error.log "config.validate: no such env file: $f"
    echo "INVALID: no such env file: $f"   # status-output idiom — verdict to stdout
    return 2
  fi
  local violations=0 lineNum=0 line trimmed
  while IFS= read -r line || [ -n "$line" ]; do
    lineNum=$((lineNum + 1))
    # Strip leading whitespace so ONLY a true blank/comment line is skipped. The
    # old `[[:space:]]*'#'*` case pattern treated any INDENTED line that merely
    # contained a '#' as a comment — e.g. `  : ${OOSH_DIR:=/x} # guarded` sailed
    # through. Trim first, then test the first real character.
    trimmed="${line#"${line%%[![:space:]]*}"}"
    case "$trimmed" in '' | '#'*) continue ;; esac
    # Reject command substitution / backticks ANYWHERE (even inside a value):
    # they EXECUTE when the file is sourced, so they are logic, never pure data.
    # `export FOO="$(echo hi)"` used to pass (the export regex only anchored to `=`).
    case "$line" in
      *'$('* | *'`'*)
        error.log "config.validate: $f:$lineNum contains command substitution (not pure data): $line"
        violations=$((violations + 1)); continue ;;
    esac
    # allow: `export IDENT=…`, `export declare IDENT=…` (legacy), bare `IDENT=…`
    if echo "$trimmed" | grep -qE '^(export[[:space:]]+(declare[[:space:]]+)?)?[A-Za-z_][A-Za-z0-9_]*='; then
      continue
    fi
    # allow: config-file chaining `source X.env` / `. X.env` (composition, not
    # logic). Anything else — self-anchors `: ${…}`, `if`/`{ } &&` — is logic.
    if echo "$trimmed" | grep -qE '^(source|\.)[[:space:]]+[^[:space:]]*\.env[[:space:]]*$'; then
      continue
    fi
    error.log "config.validate: $f:$lineNum is not pure data: $line"
    violations=$((violations + 1))
  done < "$f"
  if [ "$violations" -gt 0 ]; then
    error.log "config.validate: $f has $violations violation(s) — env files must contain only export/comment/blank lines (logic belongs in $OOSH_DIR/boot)"
    echo "INVALID: $f — $violations violation(s) (logic in an env file)"
    return 1
  fi
  echo "OK: $f is pure data"
  return 0
}

config.validate.completion.name() { echo "user oosh log"; }

private.config.required.variables.get() # # the variables a config must carry for a branch version: "<variable> <file> <deriverFunction>", one per line
{
  # NO create.result — a getter consumed as $(...), which runs in a subshell.
  # Same reasoning as private.config.file.resolve.
  #
  # This is the POSITIVE list the tree never had. config.save has always had a
  # negative one (what must never persist) and config.validate checks line
  # SHAPE while knowing no variable name at all, so nothing asserted that a
  # config contains what a branch version needs. Mirrored in docs/config.md
  # § Required variables — change both together.
  #
  # OOSH_BRANCH is deliberately absent: it is install INPUT, excluded from
  # persistence since T7, and a config that carried it would be the bug.
  cat <<'REQUIRED'
BASH_FILE user.env private.config.derive.bash.file
CONFIG_FILE user.env private.config.derive.config.file
OOSH_MODE oosh.env private.config.derive.oosh.mode
OOSH_OS oosh.env private.config.derive.oosh.os
OOSH_PM oosh.env private.config.derive.oosh.pm
LOG_LEVEL log.env private.config.derive.log.level
REQUIRED
}

config.validate.required() # # verify the persisted config carries every variable a branch version needs and that OOSH_MODE agrees with the checkout; rc 1 naming each problem
{
  # A SUB-METHOD of config.validate, not a mode argument: this.call prefers
  # $caller.$method.$1 when that function exists, so `config validate required`
  # resolves here and config.validate's own `${1:-user}` default and its
  # internal caller in config.save are untouched. (Before this existed,
  # `config validate required` silently resolved to a non-existent
  # required.env and returned 2.)
  local missing="" variable file rest
  while read -r variable file rest; do
    [ -z "$variable" ] && continue
    grep -qE "^[[:space:]]*(export[[:space:]]+)?${variable}=" "$CONFIG_PATH/$file" 2>/dev/null \
      || missing="$missing $variable"
  done <<REQUIRED
$(private.config.required.variables.get)
REQUIRED

  # Branch drift: the persisted OOSH_MODE against the branch ~/oosh actually
  # points at. This is the T7 bug — persisted prod/released on a dev box, with
  # nothing in the tree ever comparing the two.
  local drift="" persistedMode actualMode
  persistedMode=$(grep -m1 -E "^[[:space:]]*(export[[:space:]]+)?OOSH_MODE=" "$CONFIG_PATH/oosh.env" 2>/dev/null \
                  | cut -d= -f2- | tr -d '"')
  # The SAME accessor the repair uses. This was two inline steps here and two
  # more at oo:2631-2633; three answers to "which branch is this host on" could
  # drift apart, and the drift check disagreeing with the repair is the one
  # disagreement that would be hardest to see.
  actualMode=$(private.config.derive.oosh.mode 2>/dev/null)
  if [ -n "$persistedMode" ] && [ -n "$actualMode" ] && [ "$persistedMode" != "$actualMode" ]; then
    drift="OOSH_MODE=$persistedMode but ~/oosh is on $actualMode"
  fi

  # Status-output idiom: the verdict goes to stdout so it survives any
  # LOG_LEVEL or LOG_DEVICE, exactly as config.validate does.
  if [ -n "$missing" ] || [ -n "$drift" ]; then
    local reason=""
    [ -n "$missing" ] && reason="missing:$missing"
    [ -n "$drift" ] && reason="${reason:+$reason; }$drift"
    create.result 1 "INCOMPLETE: $CONFIG_PATH — $reason"
    echo "$RESULT"
    echo "  repair with: config init.env   (regenerates user.env + oosh.env + log.env)"
    return $(result)
  fi
  create.result 0 "OK: $CONFIG_PATH carries every required variable and agrees with the checkout"
  echo "$RESULT"
  return $(result)
}

config.validate.required.completion() { :; }

config.bash.minimal.version() { # <bash.minimal.version:5> # sets the BASH_MINIMUM_MAJOR_VERSION
  echo "export BASH_MINIMUM_MAJOR_VERSION=$1" >>$CONFIG
}

private.config.file.resolve() # # echo the effective file for the current $CONFIG_FILE — shared $CONFIG_PATH if present, else per-user $OOSH_USER_CONFIG_PATH
{
  # Two-tier config: the shared $CONFIG_PATH holds site-wide data (user.env,
  # oosh.env, log.env); the per-user $OOSH_USER_CONFIG_PATH holds private
  # per-user / per-session files (log.session.env). A name that exists only in
  # the per-user tier (e.g. `log.session`) resolves THERE. Callers pass through
  # config.file.check first, which sets $CONFIG_FILE. Pure echo — no global
  # mutation, so `config list log.session` never leaves $CONFIG repointed.
  local _shared="$CONFIG_PATH/$CONFIG_FILE"
  local _peruser="${OOSH_USER_CONFIG_PATH:-$HOME/.config/oosh}/$CONFIG_FILE"
  if [ ! -f "$_shared" ] && [ -f "$_peruser" ]; then
    echo "$_peruser"
  else
    echo "$_shared"
  fi
}

config.list() # <name:$CONFIG> # lists the content of the config with <name>
{
  # Listing is READ-ONLY: snapshot and restore the current-file globals so
  # `config list <name>` (which runs config.file.check, and which config.info.log
  # calls after save/clean) never leaves $CONFIG/$CONFIG_FILE repointed for the
  # rest of the shell — a later config.add/clean would otherwise write to / rm
  # the wrong file. Resolve the tier (shared, else per-user) for the view only.
  local _savedConfig="$CONFIG" _savedFile="$CONFIG_FILE"
  config.file.check $1
  cat "$(private.config.file.resolve)"
  CONFIG="$_savedConfig"; CONFIG_FILE="$_savedFile"
}

config.delete() # <name:$CONFIG> # deletes the config with <name>
{
  config.file.check $1
  # Tier-aware so `config delete log.session` removes the real per-user file
  # rather than a non-existent shared one (symmetry with config.list).
  rm "$(private.config.file.resolve)"
}

config.update() { # <?name:$CONFIG> <?ENV_PREFIX> # saves and adds the config 
  config.save $1 $2
  config.add $1 $2
  config.info.log
}

config.info.log() # # logs the config when on LOG_LEVEL info or higher
{
  if [ "$LOG_LEVEL" -gt "3" ]; then
    config.list $1
  fi
}

config.edit() # <name:$CONFIG> # edits the config with <name> in vim
{
  #check.debug.level 6
  oo cmd vim
  # Tier-aware (shared, else per-user) so editing `log.session` opens the real
  # per-user file instead of creating a SHARED shadow that then masks it for
  # every user in `config list log.session`. Symmetry with list/delete.
  local _f
  if config.file.check $1; then
    _f="$(private.config.file.resolve)"
    vim "$_f"
  else
    _f="$(private.config.file.resolve)"
    vim "$_f"
    exit $ERROR_CODE_RECONFIG
  fi
}

config.clean() #  # reinitalises a clean config and removes any custom addons
{
  config.file.check $1
  # Order-preserving de-dup (drop duplicate lines, keep first occurrence).
  # Kept order-preserving rather than `sort -u` as a matter of hygiene — env
  # files are now pure `export K="V"` data (no bootstrap header to protect;
  # that logic moved to $OOSH_DIR/boot), so order is no longer load-bearing,
  # but there's no reason to alphabetically shuffle a user's config either.
  awk '!seen[$0]++' "$CONFIG" >"$CONFIG_PATH/$CONFIG_FILE.clean"
  rm $CONFIG_PATH/$CONFIG_FILE
  mv $CONFIG_PATH/$CONFIG_FILE.clean $CONFIG_PATH/$CONFIG_FILE
  private.ensure.groupWrite "$CONFIG_PATH/$CONFIG_FILE"
  if [ "$LOG_LEVEL" -gt "3" ]; then
    config.list $1
  fi
}

config.add() # <name:$CONFIG> # adds the config <name> into the user.env permanently
{
  local file=$1
  shift
  local name=$1

  if [ -n "$file" ] &&  [ -z "$name" ]; then
    #set -x
    # NOT `${file^^}` below. That is bash 4+, and on macOS /bin/bash 3.2 it is
    # not a warning but a FATAL "bad substitution" — the shell EXITS (measured
    # on a sequoia VM; the commands after it never ran). This branch is the
    # DOCUMENTED single-argument form — `config add oosh`, `config save myapp`
    # (docs/config.md) — so on macOS a documented command killed the user's
    # shell. The `warn.log "BASH 5 support only"` that stood here was never a
    # mitigation: the line under it had already ended the shell.
    name=$(private.config.string.upper "$file")
  else
    shift
  fi

  if [ -z "$file" ]; then
    name="CONFIG"
  fi
  
  console.log "
  config.add [file:$file] [name:$name] to $CONFIG"
  {
    # POSIX `.` not bash `source` — env files are sourced by $OOSH_DIR/boot,
    # which must parse under dash/ash (/bin/sh login shells, `ossh exec` on
    # Debian). `source` is a bashism (dash: "source: not found").
    echo . \$CONFIG_PATH/$file.env
  } >>$CONFIG
  config.clean
}

config.file.check() # <name:$CONFIG> # sets the default config file to <name> if it exists
{
  if [ -n "$1" ]; then
    config.file $1.env
    shift
    RETURN=$1
    RETURN_VALUE=0
  else
    RETURN_VALUE=1
  fi
  info.log "config file: $CONFIG"
  create.result $RETURN_VALUE $CONFIG
  return "$(result)"

}

config.file() # <configfile> # sets the default config file to <configfile> if it exists
# if <?option> is "reset", it sets the $CONFIG_FILE backi to user.env
{
  if [ -z "$1" ]; then
    echo "$CONFIG_FILE"
  else
    case "$1" in
      reset)
        config.init
        echo "CONFIG_PATH=$CONFIG_PATH"
        echo "CONFIG_FILE=$CONFIG_FILE"
        echo
        echo "CONFIG=$CONFIG"
        ;;
      *)
        export FILE=$1
        CONFIG_FILE=$FILE

        export CONFIG=$CONFIG_PATH/$CONFIG_FILE
    esac
  fi
}

config.location() # <configfile> # sets the default config location to <configfile> if it exists
# if <?option> is "reset", it sets the $CONFIG_FILE backi to user.env
{
  if [ -z "$1" ]; then
    echo "$CONFIG_FILE"
  else
    case "$1" in
      reset)
        config.init
        echo "CONFIG_PATH=$CONFIG_PATH"
        echo "CONFIG_FILE=$CONFIG_FILE"
        echo
        echo "CONFIG=$CONFIG"
        ;;
      *)
        local oldConfig="$CONFIG"

        # config-path-exception: `config file <path>` deliberately points the
        # session at an ARBITRARY config file, so CONFIG_PATH must come from
        # that path — this is the one method whose job is to leave ~/config.
        export FILE=$(basename $1)
        CONFIG_PATH=$(dirname $1)
        CONFIG_FILE=$FILE

        info.log "old config     : $oldConfig"
        info.log "new config path: $CONFIG_PATH"
        info.log "new config file: $CONFIG_FILE"

        export CONFIG=$CONFIG_PATH/$CONFIG_FILE
        config.save
        bash
    esac
  fi
}

config.parameter.completion.configfile() {
  echo "reset"
  c2 file.completion "$1"
}

config.parameter.completion.envVariable() {
  cat $CONFIG | line replace "export " | line replace "=.*$" | line filter "source"
}

config.parameter.completion.name() {
  ls "$CONFIG_PATH"/*.env 2>/dev/null | xargs -I{} basename {} .env
}

config.parameter.completion.sshConfigName() {
  source ossh 2>/dev/null
  ossh.parameter.completion.sshConfigHost "$@"
}

function config.path.create() # <pathString> # (internal deprecated) creates respective paths
{
    debug.log " function ${FUNCNAME[0]}($1) $@"

    if [[ "$1" = /* ]]; then
        cd "/"
    fi

    path=""
    info.log "creating path in $(pwd): $1"

    for current in ${1//// }; do

        if [ -z "$path" ]; then
            path=$current
        else
            path=$path/$current
        fi
        debug.log "checking path: $path"

        config.folder.create $path
    done
}

function config.folder.create() # <folderName> # (internal deprecated) creates respective directory requested in parameter 
{

    local current=$1
    if [ ! -d $current ]; then
        debug.log "$current does not exist: creating it..."
        mkdir -p $current
    fi
}

config.ssh.host.set() # <sshConfigName> # configures the PROMPT and the name of this host for ssh config
{ 
  export OOSH_SSH_CONFIG_HOST=$1
  config.save
  echo "To apply changes immediatly type:
  
  exit $ERROR_CODE_RECONFIG"
  exit $ERROR_CODE_RECONFIG
  #reconfigure
}

config.get() # <envVariable>  # gets an environment variable from the user.env
{
  local envVariable="$1"
  if [ -n "$envVariable" ]; then
    shift
  else
    error.log "no envVariable specified"
    return 1
  fi

  # Matches both "export X=..." and the legacy malformed "export declare X=..." form.
  local currentDefinition="$( grep -E "^export( declare)? ${envVariable}=" "$CONFIG" 2>/dev/null )"
  info.log "declaration: $currentDefinition"
  if [ -n "$currentDefinition" ]; then
    local val="${currentDefinition#*=}"
    val="${val#\"}"
    val="${val%\"}"
    echo "$val"
  fi
}

config.set() # <envVariable> <value> # adds or sets an environment variable to the user.env 
{
  local envVariable="$1"
  if [ -n "$envVariable" ]; then
    shift
  else
    error.log "no envVariable specified"
    return 1
  fi

  local value="$1"
  if [ -n "$value" ]; then
      shift
  fi

  # Matches both "export X=..." and the legacy malformed "export declare X=..." form.
  local currentDefinition="$( grep -E "^export( declare)? ${envVariable}=" "$CONFIG" 2>/dev/null )"
  if [ -n "$currentDefinition" ]; then
    if [ -z "$value" ]; then
      warn.log "set blank value on
      $currentDefinition

      "
    fi
    local tmpFile="${CONFIG}.tmp.$$"
    # Replace the whole line (any form) with the clean "export X=value" form — normalizes on update.
    sed -E "s|^export( declare)? ${envVariable}=.*|export ${envVariable}=\"${value}\"|" "$CONFIG" > "$tmpFile" && mv "$tmpFile" "$CONFIG"
  else
    echo "export $envVariable=\"$value\"" >>$CONFIG
  fi
}


config.unset() # <envVariable> # removes <envVariable> from the current $CONFIG file (idempotent)
{
  local envVariable="$1"
  if [ -n "$envVariable" ]; then
    shift
  else
    error.log "no envVariable specified"
    return 1
  fi

  [ ! -f "$CONFIG" ] && return 0

  if grep -q "^export $envVariable=" "$CONFIG" 2>/dev/null; then
    local tmpFile="${CONFIG}.tmp.$$"
    grep -v "^export $envVariable=" "$CONFIG" > "$tmpFile" && mv "$tmpFile" "$CONFIG"
    debug.log "config.unset: removed $envVariable from $CONFIG"
  fi
}


config.discover() # # lists the existing config files and where they are
{
  echo $CONFIG_PATH
  ls $CONFIG_PATH
}


function config.start() # <method> <parameter> # default start and parameter processing
{
  #echo "sourcing init"
  COMMANDS="$@"
  source this
  #check.debug.level 6

  if [ -z "$CONFIG_PATH" ] || ! [ -f "$CONFIG" ]; then
      config.init
  fi
  if [ "$(type -t this.start)" = "function" ]; then
    this.start "$@"
  else
    echo "config.start: this.start not yet there"
  fi
}

config.start "$@"

