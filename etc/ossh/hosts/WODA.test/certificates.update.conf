# ossh certificates.update — per-host config for WODA.test
#
# Loaded by private.certificates.update.config.load via `source`.
# Keys map 1:1 to the CERT_UPDATE_* variables in the state machine.

domain=test.wo-da.de
workspace=/var/dev/EAMD.ucp/Scenarios/de/1blu/v36421/vhosts/de/wo-da/test/EAM/2_systems/Docker/StructrServer.v2.1.4/Workspaces/structrAppWorkspace
certScenario=certbot
appScenario=structr
dataDir=WODA-current

# Optional — defaults shown
backupBase=/var/backups
verifyMinDaysLeft=30
