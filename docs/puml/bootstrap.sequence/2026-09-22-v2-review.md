# Bootstrap Sequence v2 — review against dev code (2026-09-22)

Reviewed: `docs/puml/bootstrap.sequence/Bootstrap Sequence v2.drawio` (12 groups, 17 lifelines,
conflict table C1–C8) against dev @ `09020ba`, and against the boss's latest `oosh.drawio`
export (`~/Pictures/oosh.webp`, 9559×8039). Every label in groups 0–11 and every row of the
conflict table was checked against the code; line numbers below are in the dev worktree.

## Verdict

**v2 is substantially right.** Of ~230 factual labels, 8 are wrong, ~25 are partial or
imprecise, and the rest match the code. The conflict table (C1–C8) is correct in 7 of 8 rows;
C1 needs one correction. The design decision behind v2 — mirror the boss's tree panels as
"draw.io says", put the code truth beside them, and give each conflict a number — is the right
one and should stay. What follows is the delta to make it *completely* correct.

The single most important finding is not in the diagram at all: **the install flow cannot
propagate a stuck state machine** (see § Code defects, D1). The diagram currently promises
that it can (n-346).

> **Status 2026-09-22 (later the same day):** W1–W8, the C1 correction, O1–O7, M1–M16 and the wording
> nits are **applied** in `Bootstrap Sequence v2.drawio` (header now reads V2.1); group 0 follows the
> boss's new entry-point tree; C9–C11 were added as a second conflict table to the right of C1–C8.
> Still open: § 4 code/doc defects D1–D10 (tickets) and § 5 housekeeping (which drawio is the source,
> rename, D8 link repair, reconnecting the free-floating arrows).

---

## 1 · Must fix in v2 (label is WRONG)

| # | Node | Says | Code does | Where |
|---|------|------|-----------|-------|
| W1 | n-340 | `loop — state.entry.add silent …` | No loop, no `state.entry.add`. 25 sequential `state.add <name> silent` calls (name first, flag second). | `oo:1571-1598`, `state:732` |
| W2 | n-342 | `state.transition.next (→ all.states.added)` | Method is `state.next`. Target [3] all.states.added is right. | `oo:1600`, `state:180` |
| W3 | n-346 | `stuck → return 1, propagated to init/oosh` | The `while` loop and `return 1` live in `oo.state` (max 30 iterations), but `ossh.install.continue.local` calls `oo state` bare, never reads its rc, and always ends `create.result 0 …; return $(result)`. init/oosh therefore always sees rc=0 and runs `user oosh.install` + `exec bash -l` regardless. | `oo:1539-1556`, `ossh:711`, `ossh:788-789`, `init/oosh:604` |
| W4 | n-353 | `user ssh.backup $USER.$local.for.$remote (if not yet backed up)` | `user ssh.backup pre-user-init`, timestamped archive under `~/.ssh.backups/`, **no guard**. The `.for.` name and the guard were removed on purpose (comment at ossh:725-737). Note T6's 2026-09-16 pass already corrected this label in the *old* drawio; v2 regressed it. | `ossh:738`, `user:1459` |
| W5 | n-222 | `exec "$BASH_FILE" -l after install` under **ssh → as root → bash** | The exec is gated on `[ -z "$MODE" ]`; `mode root` sets MODE=root, so the ssh path **never** execs a login shell. Group 10's n-1224 already says this correctly — n-222 contradicts it. Move the exec under the *git/curl* entry, or label it "curl flow only". | `init/oosh:435`, `init/oosh:661-676` |
| W6 | n-453 | `return — idempotent: re-sourcing never grows PATH` | Only the `$OOSH_DIR` block is a colon-anchored segment test. The `$BASH_FILE` block is front-anchored only, so with BASH_FILE set a re-source **can** grow PATH by one entry — stated in boot's own header. Also boot ends with `:`, not `return`. | `boot:13-18`, `boot:107-118`, `boot:155`, docs/boot.md "Idempotent" |
| W7 | n-957 / n-958 | log tree: `level → call log.init` | `log.level` never calls `log.init`. The only caller of `log.init` is `log.start` (when LOG_LEVEL is empty). The rest of n-958 (check.debug.level, started → `config save log LOG`, exit) is right. | `log:377-425`, `log:711-712` |
| W8 | n-996 | `the one place log writes the shared log.env` | Three places call `this.call config save log LOG`: `log.level` (399), `log.live.file` (473), the *second* `log.device` (586). | `log:399,473,586` |

**C1 correction (n-41):** "USERHOME and HOME are never set anywhere" → USERHOME is never set;
**HOME is set by boot's step 0** (`homeRecovery`, `boot:28-60`, duplicated in
`init/oosh:51-88`) when HOME is unset or not a directory. `this` never sets it — n-520 stays.

## 2 · Should fix in v2 (PARTIAL — the mechanism is different from what is drawn)

**Ordering errors**

| # | Where | Diagram order | Code order |
|---|-------|---------------|------------|
| O1 | group 7, n-1061→n-1063→n-1065 and left tree n-1034→n-1038→n-1042 | config.init → source this → this.start | `COMMANDS="$@"` → **`source this`** → `[ -z CONFIG_PATH ‖ ! -f CONFIG ] config.init` → this.start. Left tree drops config.init entirely. | 
| O2 | group 3 .bashrc tail, n-456…n-462 | line init → sudo guard → c2.install → trap → .once → **PS1** | boot → line init/color → sudo cwd guard (all *inside* the boot-present `if`) → **PS1 export (:205-209)** → c2.install (:234) → onExit + trap (:236-250) → `reconfigure()` (:252) → .once (:259) → `OOSH_PROMPT="oosh "` (:266) → nvm → welcome banner. The `[oosh ]` prefix comes from the *late* OOSH_PROMPT export, not from PS1. |
| O3 | group 2, n-299/n-300 inside "Phase A" | auto-run guard and OOSH_BRANCH are Phase A steps | Both are in a **pre-prelude** before the Phase A marker (`init/oosh:38-40`, `:96-97`), followed by `$HOME` recovery (`:51-88`) and the `exec env -i … OOSH_CLEAN_ENV=1 sh $0` clean-env re-exec (`:142-171`). Branch *sanitisation* happens in Phase B after arg parsing (`:444-448`). |
| O4 | group 2, n-335→n-337 | `oo state` directly after continue.local starts | **`oo update` runs first** (`ossh:709`) — a GitHub pull in the middle of the install. Then `oo state`. |
| O5 | group 2, n-344→n-346 | machine.start → loop | `private.init.state.machine` advances three states itself before returning to the loop: [11] remote.install.started → [12] local.install.started → [13] priviledges.checked (`oo:1602-1606`). `private.init.state.machine` only runs when the machine does not exist; on re-runs `oo.state` does `state of SETUP_SERVER list all` (`oo:1526-1535`). |
| O6 | group 10, n-1226 | push.config first | `private.ossh.push.config $host` with **one arg is a documented no-op** (`ossh:2097-2098`). Real sequence: seed.github → shared.ssh.copy.to.all → get.key.name/pull.key → **`ossh.update.authorized_keys`** (`ossh:838`, missing) → install.user.remote → connection.close. |
| O7 | group 4 top-level, n-524/n-755 | LOG_DEVICE·SUDO·USER after EXPECTED_RETURN_VALUE | PS4/SH_OPT → umask → LOG_LEVEL → LOG_DEVICE → EXPECTED_RETURN_VALUE → OOSH_DIR fallback → SUDO → USER (`this:6-77`). Minor. |

**Missing steps that change the picture**

| # | Group | What is missing | Where |
|---|-------|-----------------|-------|
| M1 | 3 | **boot step 0 — HOME recovery** (getent → dscl → /etc/passwd, refuse with `return 1`). docs/boot.md numbers it 0; the diagram starts at 1 and then numbers log 6 and session.save 7, whereas docs/boot.md has 6 = log + session.save, 7 = exit 0. Pick one numbering (recommend docs/boot.md's). | `boot:20-61`, docs/boot.md:71-96 |
| M2 | 3 | The `[ -n $BASH_VERSION ]` guard is really `[ -n "$BASH_VERSION" ] && [ "$_oosh_posix" = no ]` via `$SHELLOPTS` — **POSIX-mode bash (macOS /bin/sh = bash 3.2) also skips log**, not just dash/ash. | `boot:129-147` |
| M3 | 3 | Second entry into boot: `/etc/profile.d/oosh.sh` boots `env -i sh -l` via `@SYSTEM_PATH@/boot` when `$HOME` is unset or `$HOME/oosh` exists. Never shown. | `templates/user/profile.d.oosh.sh:27-31`, state 34 (`oo:333-352`) |
| M4 | 2/10 | After `config ssh.host.set`: `user ssh.backup pre-user-init` → `user init` → move `$HOME/*.public_key` into `~/.ssh/public_keys` → `user authorized.keys.update` → `osshLayout build` (warn-only) → `unset LOG_INSTALL INSTALL_LOG`. | `ossh:738-782` |
| M5 | 2/10 | State 31 seed call is `ossh config.shared.github.create`; `ossh config.shared.create` (n-348) is DEPRECATED. State 31 also: wget developking.ssh from test.wo-da.de with template fallback, pre-install `user ssh.backup pre-install`, Host github.com write, `git clone git@github.com:… main` + worktree for $OOSH_BRANCH, config relocation, `ln -s ~/oosh/init ~/init`, 5 fail-loud checkpoints. | `oo:2033-2563`, `ossh:1737` |
| M6 | 10 | Remote specifics: on the remote `$0=./oosh` has no `../.git`, so OOSH_DIR resolves straight to `$HOME/oosh` — **no mv, no re-exec** (n-1211 shows both). The transferred `~/oosh` file is `rm -f`'d only when the ssh user *is* root; after `sudo -H`, HOME=/root so a non-root user's stale file stays until `user oosh.install` removes it (`user:1024`). | `init/oosh:521-528`, `:539-542`, `:564-574` |
| M7 | 10 | n-1198: `ossh prereqs.install <host>` is **deprecated** (`ossh:2715`) and runs the *whole* installer (`bash /tmp/oosh-init.$$`, no args → MODE empty → clone + continue.local + state machine), not "Phase A only". Phase A installs git and bash 4+ (plus Homebrew bootstrap / apt update); it does **not** install curl, shadow or util-linux (init/oosh:16-19 header is stale). | `ossh:2724-2739`, `init/oosh:322-369` |
| M8 | 5 | onError: first step is `[ -n "$1" ] && RETURN_VALUE=$1`; "GOT EXPECTED ERROR" warn only when `RETURN_VALUE ≠ 1`; cyan "applying new configuration" echo precedes the restart; nested `errno()`. Traps are armed only when `LOG_LEVEL -lt 5` (n-763 "traps armed" should say so). | `debug:237-295`, `debug:326-329` |
| M9 | 6 | `log.device` is defined **twice** (57, 582); the second wins and persists via `config save log LOG`. `err.log` (293-320) is the real `error.log` and has its own cold-path `source $CONFIG`. Missing from the function list: err.log, log.capture.result(.silent), log.inspect(.errors), log.clear.*, log.cls, log.live.file. | `log` |
| M10 | 7 | config.save is not a "pure persister": carry-forward loop re-exports required vars from the old file (909-919), `log.device $LOG_DEVICE` HACK (962-964, which recursively saves log LOG), `config.file user.env` (978), config.add → config.clean (1271), INVALID verdict → stderr + rc 1. Exclusion list also has `CONFIG`, `OOSH_COMPONENTS_DIR`, `_` (615). | `config` |
| M11 | 4/5/7 | **`this` has no "already sourced" early-return guard.** n-827 ("returns at once when already sourced") and n-1063 ("returns early") are wrong: every `source this` (debug:16, log:139, config:1496) re-runs this.init, the PATH case and the isSourced gate; only the *started* branch is skipped. n-759 ("this was already sourced → return 0") is inside `this.load`, not the file top level. | `this:500-503`, `this:1247-1294` |
| M12 | 9 | this.restart sources the literal `~/config/result.env`, not `result.load`/`$CONFIG_PATH` (n-1173, n-1142). n-1177 "→ exit $RETURN_VALUE": there is no exit there — this.start `return`s (1237) or the chaining loop `exit`s (1226). | `this:1118-1123`, `this:1201-1203` |
| M13 | 3 | n-426: only USER and SUDO are *exported* before the interactive check; EXPECTED_RETURN_VALUE and CONFIG are plain assignments. | `bashrcTemplate:6-25` |
| M14 | 2 | n-321 `oo cmd — rsync, tree`: prereqs.install **sources** `oo` and calls `oo.cmd` as a function (deliberately not the subprocess), per-package failure → warn.log. Local mode always `return 0`, so "rc — warn only" is trivially true. | `ossh:2693-2698` |
| M15 | 0/1 | n-178/n-190 `… \| sh` pipe forms are not documented; README documents `sh -c "$(curl -fsSL …)"` / `"$(wget -O- …)"` / `"$(fetch -o - …)"` and `unbuffer env -i sh -xc "$(wget …)" \| tee`. n-241 macDnD: the repo's macOS path is **`Install oosh.command`** (repo root, double-click; runs sibling `init/oosh` or fetches it), not "drag init/oosh onto a terminal". promote rewrites its `OOSH_SELF_BRANCH` literal too. | README.md:35-59, `Install oosh.command:36-52`, `promote:624-652` |
| M16 | 11 | deinstall leaves behind `/etc/oosh/boot`, `/etc/profile.d/oosh.sh`, `~/.bash_profile`, `~/.ssh.backups/`, `~/.ssh/ids/`, `~/.bashrc.pre-oosh`, the `dev` group, root's login-shell change — and the developking *account* (see D2). "bare shell restored" (n-1261) overstates it. | `init/deinstall.oosh`, `oo:2572-2609` |

**Wording nits:** n-782 (this.call) has lost tokens (`. [. ]`, `[ exists]`) — see this:918-972 for the real
resolution order (function → `$caller.$fn` / `$caller.$fn.$1` → script+method via this.load → methodNotFound 127).
n-780 `This=$(command -v )` lost `$caller`. n-434 `: > log.session.env` only when the file is missing.
n-1058 / n-984 / n-1118 "dev addition(s)": these exist on prod and testing too — say "added since the draw.io".
n-4 header still says `docs/puml/bootsratp.sequence.puml` (typo'd, pre-rename path).

## 3 · The boss's diagram — what changed, and where it disagrees with the code

**Changed in his latest export (vs the copy v2 was aligned to):**

1. **OOSH Entry Points:** root box is now `init/oosh` (was `oosh`). `env -i sh` (red) is a **direct child of
   init/oosh** with `as root → bash | this` and `as user`; it is no longer under `ssh`. `ssh → <sshConfig>` is now
   its own orange branch, and `os platform` is green. **v2 group 0 still nests `env -i sh` under `ssh`** (n-202 →
   n-210) and has no `<sshConfig>` box — restructure to match him, keeping the C4 annotation on `as user`.
2. Everything else in his bootstrap-related panels (ossh boot sequence, this, debug, log, result system,
   config Tree Container) is unchanged. The `oo basics`, `state → machine → list → testcase/broken/macos.latest`,
   `SETUP_SERVER_STATES state machine` and `odocker` panels are unchanged too and are **not** part of the
   bootstrap sequence; v2 rightly leaves them out. (The agents/LLM swimlanes at the top are a different diagram.)

**Where his panels disagree with dev (for him to update, or for v2's conflict table):**

| Panel | Box | Dev |
|-------|-----|-----|
| Entry Points | `env -i sh → as user` | Gone: only `mode root`; non-root re-execs `sudo -H -E sh "$0"` (C4 ✓). |
| Entry Points | `env -i sh → as root → bash` | No `bash -l` in the ssh/`mode root` path (W5). |
| this | `init → USERHOME`, `init → HOME` | USERHOME never set; HOME set only by boot step 0 (C1, corrected). |
| this | `this.isSourced false → config ci` | `config init` (C8 ✓). |
| this | `restart → duplicate of this.restart?` | Two halves of one loop (C3 ✓). |
| this | `args → call / discover` | Commented out (C6 ✓). |
| this | `create result` | `create.result`, `result`, `result.save/load/into/cat/show/view` — his "result system from log" panel puts them under `this`, which is right; the *log* in its title is the misnomer. |
| log | `init (red) → LOG_DEVICE` then a second `init` | Matches: two `log.init` definitions, second wins — but `log.device` is **also** defined twice (M9), not shown. |
| log | `level → call log.init` | Does not exist (W7). |
| log | `init → init config / source config` | Cold path only, `[CONFIG empty]` (C7 ✓). |
| config Tree Container | list/save/init/start/add/get/set/discover | Missing on his side: `validate`, `file`, `init.shared/user/check/env/full`, `ssh.host.set`, `edit`, `clean`, `delete`, `update`, `location`, `unset`. `start → source this` should be `source this → config.init → this.start` (O1). |
| oo basics | `prereq.install` | `oo prereqs.install` (plural). `status` does not exist as an oo method (there is `oo boot.status`, `oo promote.status`). `install.dev` is DEPRECATED. `mode` also has `stage`; oo also has `boot.fix`, `cmd`, `deinstall`, `install`, `method.new/delete`, `new`, `pm`, `promote.*`, `release` (deprecated), `remote.update`, `safeDirectory.prune`, `su`, `test.new`, `use`. |
| Tree Container | `SETUP_SERVER_STATES state machine` | The machine is named `SETUP_SERVER` (`oo:1524`, `~/config/stateMachines/SETUP_SERVER.states.env`); `SETUP_SERVER_STATES` is the *array* name inside it. `state machine list` lists files, there are no machines called `testcase`/`broken`/`macos.latest` — those look like TODO markers, not tree nodes. |
| odocker | workspace/repositories/container/image/networks/volumes/compose | Wish-list, not code: no `odocker.repositories.*`, no `container.sshConfig.*`, no `image.push/pull`, no `networks.*`, no `volumes.*`; `start/stop/up/down/enter/build` are top-level verbs, not under `container`/`image`. Out of scope for the boot sequence, noted for completeness. |

## 4 · Code and doc defects found on the way (not diagram work — ticket material)

| # | Defect | Where |
|---|--------|-------|
| D1 | **`oo state` rc is discarded in `ossh.install.continue.local`**; a stuck SETUP_SERVER machine still yields rc=0, so init/oosh runs `user oosh.install` and `exec bash -l` on a half-installed host. The comment at oo:1548-1552 claiming propagation is wrong. Fix belongs in the state framework / continue.local per "state machine owns install". | `ossh:711`, `ossh:788-789`, `oo:1539-1556` |
| D2 | `init/deinstall.oosh:3` calls `user delete.linux developking` — **no such method** (it is `user linux.delete`); `this.methodNotFound` → 127, masked by `‖ true`. developking is never deleted, only its home is `rm -rf`'d. | `init/deinstall.oosh:3`, `user:1296` |
| D3 | `this:1319` `ln -s $OOSH_DIR/oosh/init` is a **one-arg `ln -s`** (creates `./init` in the CWD) and `$OOSH_DIR/oosh/init` does not exist. Dead/broken code, only unreachable because `init/once` exists. | `this:1317-1321` |
| D4 | `this:1372` `export SHELL=$BASH_FILES` typo (known, C8). | `this:1372` |
| D5 | `debug:98` step `r` calls `removeTrap`, which is **not defined** anywhere in the current tree (only `old/debug:231`). | `debug:98` |
| D6 | docs/state.md:190-205 still documents `state.entry.add` / `state.transition.next` — neither exists (`state.add`, `state.next`). This is where W1/W2 came from. | `docs/state.md` |
| D7 | docs/debug.md refers to `debug.setTrap` / `debug.onError` / `debug.onReturn`; the functions are unprefixed. | `docs/debug.md:86,110-116,275` |
| D8 | **Broken links after 4e915d1:** `docs/wiki-index.md:46` and `docs/boot.md:522` link `puml/bootstrap.sequence.drawio`, which moved to `puml/bootstrap.sequence/bootstrap.sequence.drawio`. `docs/plans/2026-09-10-oosh-boot-tickets.md` (T6) likewise. | `docs/wiki-index.md`, `docs/boot.md` |
| D9 | `init/oosh:4-5,16-19` header comments are stale (`bash -c`, "SCPs", curl/shadow/util-linux). `init/oosh:540` `info.log 2>/dev/null ‖ true` is a no-op in POSIX sh. | `init/oosh` |
| D10 | config.save's `log.device $LOG_DEVICE` HACK (config:962-964) triggers a recursive `config save log LOG` via `log:586`. Works, but it is why log.env is written more than once per save. | `config:962`, `log:582-590` |

## 5 · Housekeeping for the diagram files

- Two drawio files now sit in `docs/puml/bootstrap.sequence/`: the six-lane `bootstrap.sequence.drawio`
  (declared the source of truth by T6 on 2026-09-16) and `Bootstrap Sequence v2.drawio`. Decide which is
  the source; if v2 wins, rename it to the family name (`bootstrap.sequence.drawio`, no spaces), mark the
  old one superseded in its header like the `.puml`, and fix D8 to point at it.
- v2's 742 edges are all free-floating (absolute points, no `source`/`target`) — that is how Claude Design
  exported it. Moving a box in diagrams.net will not move its arrows. Fine for a rendered picture; painful
  for maintenance. Reconnecting the ~40 arrows on the main spine (groups 2–4) would be enough.
- Keep the C-table. Add rows for the new conflicts this review surfaced: **C9** `level → call log.init`
  (W7), **C10** `config start → source this` order (O1), **C11** HOME set by boot step 0 (C1 amendment).

## 6 · Hand-off — for the next agent

1. Apply W1–W8 and the C1 wording change in v2 (label edits only; node ids above).
2. Restructure group 0 to the boss's new entry-point tree (`init/oosh` root, `env -i sh` sibling of `ssh`,
   `ssh → <sshConfig>`), keep the C4 annotation.
3. Add M1 (boot step 0), M2 (POSIX-mode bash), M3 (`/etc/profile.d/oosh.sh` route), M4 (continue.local
   tail), O4/O5 (`oo update`, three state advancements). Renumber boot steps to docs/boot.md's 0–7.
4. Fix O1, O2, O6 ordering; drop the no-op push.config or mark it "no-op (one arg)".
5. Open tickets for D1 (highest — install can silently succeed on a stuck machine), D2, D3, D5, D6/D7/D8.
   D1 belongs in the state framework, not in `finish.local` (see memory: state machine owns install).
6. Resolve the two-drawio question (§5) and repoint D8's links. Then regenerate no `.svg` — T6 said the
   drawio is the source and a render pipeline is a separate tooling decision; that still holds.
7. Send the boss the "his panels vs dev" table in §3; the `SETUP_SERVER_STATES` name, the `oo status` /
   `prereq.install` boxes and the config tree are the ones he can act on quickly.
